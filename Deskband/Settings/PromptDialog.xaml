<Window x:Class="Deskband.Settings.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:conv="clr-namespace:Deskband.Common.ValueConverters"
        xmlns:ext="clr-namespace:Deskband.Common.MarkupExtensions"
        mc:Ignorable="d" 
        d:DesignHeight="400" d:DesignWidth="100"
        TextOptions.TextFormattingMode="Display"
        Title="Deskband Controls Prompt"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="CommandButton" TargetType="Button">
            <Setter Property="Margin" Value="5 5 0 0" />
            <Setter Property="Padding" Value="2" />
            <Setter Property="Width" Value="70" />
        </Style>
    </Window.Resources>
    <StackPanel Margin="5" Width="400">
        <TextBlock Name="txtQuestion" Margin="5"/>
        <TextBox Name="txtResponse" Margin="5"/>
        <PasswordBox Name="txtPasswordResponse" Margin="5"/>
        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
            <Button Style="{StaticResource CommandButton}" Content="_Ok"
                    IsDefault="True" Name="btnOk" Click="btnOk_Click" />
            <Button Style="{StaticResource CommandButton}" Content="_Cancel"
                    IsCancel="True" Name="btnCancel" Click="btnCancel_Click" />
        </StackPanel>
    </StackPanel>
</Window>
